const inputArray = [100, 10, 20, 40];

// write your codes

const answer = inputArray.map(num => num + "%")
console.log(answer)

// const answer = inputArray.map((num) => 
//   num + "%"
// )
// const answer = inputArray.map((num) => {
//   return num + "%"
// })